[Window]
WINDOW_W = 1920
WINDOW_H = 1080
FULLSCREEN = true

[Quality]
ATLAS_UPSCALE_MUL = 2.0 ; render the atlas at this much size multiplier for better quality when scaling down on draw
ATLAS_PAD_PX = 8        ; padding in pixels around each card in the atlas for texture bleeding
FONT_BASE_LOAD = 108    ; base font size to load the symbol font with for better scaling down quality
FONT_BASE_MUL = 6.8     ; base font size multiplier after scaling

[Options]
CARD_REAR_THEME_IDX = 3 ; index of the card rear theme to use